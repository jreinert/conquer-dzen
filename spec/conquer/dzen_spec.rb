require 'spec_helper'

describe Conquer::Dzen do
  it 'has a version number' do
    expect(Conquer::Dzen::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
